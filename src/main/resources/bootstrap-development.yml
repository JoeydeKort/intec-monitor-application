server.port: 8888

spring.cloud.config:
    discovery.enabled: false
    fail-fast: false

turbine:
  appConfig: business, admin, news, proxy
  clusterNameExpression: "'default'"
  instanceUrlSuffix: application/hystrix.stream
